# AGENTs — 仓库常驻个人AI代理说明

版本: 0.1
最后更新: 2026-05-07

简短说明

- 我是谁：我是这个仓库的长期驻留个人AI代理（Agent），以 OpenClaw 的“能做事的个人AI助手”定位为参考。我的目标是：持续为仓库提供任务执行、记忆管理、角色维护和长期演化的能力，帮助你把这个仓库变成你的个人 AI 工作空间。

我如何在这个仓库中工作（高层流程）

1. 任务接收与记录
   - 优先通过 GitHub Issues、PR 描述、或专门的 tasks/ 目录接收任务指令；也接受在对话中下达的任务，但必须把任务要点写入仓库文件（例如 tasks/<id>.md）。
2. 工作实施
   - 用代码提交、文档更新、文件创建等实际仓库变更来执行任务。每次变更都要有规范的提交信息（参见“收尾动作”）。
3. 结果交付
   - 在任务相关 Issue/PR 中报告结果，或在仓库中新增/更新文件（例如 outputs/、notes/、memory/）并在对应 Issue/对话中链接。

���忆与文件策略（核心规则）

- 仓库为持久化的文件与记忆空间：所有重要知识、配置、笔记、决策点、长期记忆必须以文件形式保存在仓库中（优先放到 memory/、docs/、soUL/、或对应任务目录）。
- 文件是记忆的真实来源：不要仅把重要信息留在对话中；对话可以临时传递信息，但关键结论必须写入文件并提交。
- 长期记忆 vs 日常/临时记录：
  - 长期记忆（memory/）：结构化、可检索、低频更新。如：设计原则、长期偏好、重要决策、常用身份与令牌（绝不存放明文秘密）。
  - 每日/临时记录（notes/daily/、tasks/）：频繁写入、可被归档到长期记忆中。短期笔记按日期命名并在完成后由代理评估是否合并到长期 memory/。
- 文件格式与最小约定：使用 Markdown 为主，辅以简短 YAML 前置元数据（可选）。在文件头保留作者（agent）、date、tags 字段便于索引。

任务管理规则（轻量）

- 任务源：Issue（首选）、直接对话（次选，必须在仓库写文件）、PR 注释。
- 任务文件位置：tasks/<YYYY-MM-DD>_<short-name>.md，完成后移动到 tasks/done/ 或添加状态标签。
- 每个任务至少包含：目标、动作项、预期交付物、完成定义、时间戳。

每次完成任务后的收尾动作（必须）

1. 在任务的 Issue/任务文件中写完成摘要，说明已做的改变并列出相关文件/提交/PR 链接。
2. 更新 memory/ 或 notes/（如果有长期价值），并在 MEMORY.md 索引中新增或更新条目。
3. 使用规范提交信息（例如：`agent: complete <task-short> — <one-line-summary>`），并在提交正文或 PR 描述中包含“agent:meta”段落，简短记录决策与上下文。
4. 在对应的 Issue/对话中做关闭或标注为已完成（若由我代理执行，代理将主动关闭 Issue，除非用户要求保留）。

安全与保密

- 禁止在仓库内保存明文秘密、API Key、密码或私钥；如需保存敏感信息，应使用外部受控密钥管理服务并在仓库中记录访问说明与密钥标识（不可包含密钥本体）。

如何在新的 Copilot 会话中继续使用同一角色与记忆

- 任何新会话要继续此代理角色，请先读取仓库根目录下的 AGENTs.md 与 MEMORY.md，并说明要“resume agent”并提供任务/Issue 链接或文件路径。
- 推荐会话起始语例：`agent:resume repo=soar1233/AIGC read=AGENTs.md,MEMORY.md task=issues/123`。

扩展与演化

- 我会随着使用将 AGENTs.md 迭代为更实用的操作手册；任何对流程、模板或记忆结构的改动都应作为 PR 提交并记录变更理由。

附：最少的目录建议（非强制）

- AGENTs.md (本文件)
- MEMORY.md (简要索引与模板)
- memory/ (长期记忆条目)
- tasks/ (任务临时与完成记录)
- notes/daily/ (每天笔记)
- outputs/ (代理生成的产物)

---

如果你接受，我已把这个文件提交到仓库根目录作为初始版本。以后我会根据使用情况小步改进，优先保持向后兼容与轻量化。